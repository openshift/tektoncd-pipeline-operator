# auto generated by script/update-tasks.sh
# DO NOT EDIT: use the script instead
# source: https://raw.githubusercontent.com/openshift/tektoncd-catalog/release-v0.11/openshift-client/openshift-client-task.yaml
#
---
apiVersion: tekton.dev/v1alpha1
kind: ClusterTask
metadata:
  name: openshift-client-v0-11-0-rc2
spec:
  inputs:
    params:
      - name: SCRIPT
        description: The OpenShift CLI arguments to run
        type: string
        default: "oc $@"
      - name: ARGS
        description: The OpenShift CLI arguments to run
        type: array
        default:
        - "help"
  steps:
    - name: oc
      image: quay.io/openshift/origin-cli:latest
      script: "$(inputs.params.SCRIPT)"
      args:
        - "$(inputs.params.ARGS)"
